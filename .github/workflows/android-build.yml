name: Build Android APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 配置 JDK 17
      uses: actions/setup-java@v4 # 建议 setup-java 也同步更新到 v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: 赋予 Gradle 执行权限
      run: chmod +x ./gradlew

    - name: 编译 Debug APK
      run: ./gradlew assembleDebug

    - name: 上传 APK 文件
      uses: actions/upload-artifact@v4
      with:
        name: TVBus-App-Debug
        path: app/build/outputs/apk/debug/*.apk
